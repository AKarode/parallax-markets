---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/PROJECT.md
autonomous: true
must_haves:
  truths:
    - "ROADMAP.md describes 5 phases matching the actual pivot architecture (contract registry, prediction persistence, paper trading eval, deployment fixes, second thesis)"
    - "REQUIREMENTS.md requirements reference actual modules that exist in the codebase (not EventBus, TickOrchestrator, 50 agents, H3 viz)"
    - "PROJECT.md validated section only lists things that actually exist post-pruning"
    - "STATE.md reflects the current position accurately (quick task completed, dead code pruned)"
    - "No document references deleted modules (agents/, simulation/engine.py, spatial/h3_utils.py, frontend/, ingestion/gdelt.py, ingestion/dedup.py, db/queries.py)"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "5-phase roadmap for prediction market edge-finder"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Requirements mapped to new 5 phases"
    - path: ".planning/PROJECT.md"
      provides: "Accurate project description with validated/active/out-of-scope reflecting reality"
    - path: ".planning/STATE.md"
      provides: "Updated project state with dead code pruning noted, correct position"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "Requirement IDs referenced in phase details"
      pattern: "REG-|PERS-|EVAL-|DEPLOY-|THESIS-"
---

<objective>
Rewrite all 4 planning documents (ROADMAP.md, REQUIREMENTS.md, STATE.md, PROJECT.md) to reflect the actual codebase after the architecture pivot and dead code pruning.

Purpose: The current planning docs describe a 50-agent swarm with EventBus, TickOrchestrator, H3 spatial viz, and frontend dashboard -- none of which exist. The actual system is a CLI-first prediction market edge-finder with 3 models, Kalshi/Polymarket integration, and paper trading. The docs must match reality so future GSD phases plan against what actually exists.

Output: 4 updated planning documents that accurately describe the current system and future roadmap.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md (SOURCE OF TRUTH for what exists)
@.planning/research/contract-mapping/RESEARCH.md (Phase 1 research)
@.planning/PROJECT.md (current -- needs rewrite)
@.planning/ROADMAP.md (current -- completely stale)
@.planning/REQUIREMENTS.md (current -- stale)
@.planning/STATE.md (current -- mostly ok, needs updates)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite PROJECT.md and STATE.md to reflect current architecture</name>
  <files>.planning/PROJECT.md, .planning/STATE.md</files>
  <action>
Rewrite PROJECT.md completely. Use CLAUDE.md as the source of truth for what exists.

PROJECT.md changes:
- "What This Is" section: Keep the prediction market edge-finder framing. Remove any mention of "agents", "EventBus", "TickOrchestrator", "H3 spatial", "deck.gl", "frontend dashboard", "WebSocket".
- "Current Milestone": Change to "v1.1 Contract Alignment + Evaluation" -- the edge-finder is BUILT, next step is making it trustworthy.
- "Target features" for new milestone: Contract registry with proxy classification, prediction persistence with full provenance, paper trading evaluation at contract level, API endpoint hydration, second thesis expansion.
- "Validated" section: ONLY list what actually exists post-pruning. Confirmed modules:
  - DuckDB schema (12 tables) + single-writer DbWriter
  - YAML scenario config for cascade parameters
  - Cascade engine: 6 parameterized rules (blockade->flow->bypass->price->downstream->insurance)
  - Google News RSS poller (primary news source, free, 5-15min)
  - GDELT DOC 2.0 API poller (secondary, free, 15-60min)
  - EIA API v2 oil price fetcher (Brent + WTI)
  - 3 prediction models: OilPricePredictor, CeasefirePredictor, HormuzReopeningPredictor
  - Kalshi API client with RSA-PSS auth (production reads, demo paper trades)
  - Polymarket read-only client
  - DivergenceDetector comparing model vs market probabilities
  - PaperTradeTracker for paper trade P&L
  - BudgetTracker with $20/day cap
  - CLI entry point (parallax.cli.brief) running full pipeline
  - FastAPI server with 6 endpoints (stub -- not hydrated)
  - Docker Compose for backend
  - 120 tests passing
- REMOVE from validated: H3 spatial utilities, WorldState dirty-set tracking, Simulation engine DES, Circuit breaker, Agent swarm (50 agents), Event->agent router, Semantic dedup, React/Vite frontend, deck.gl map, WebSocket hook, 3-column dashboard
- "Active" section: Replace with the 5 new roadmap phase targets (contract registry, prediction persistence, paper trading eval, deployment, second thesis)
- "Out of Scope": Keep relevant items (multi-user, mobile, real-money trading). Remove items about 50 agents. Add: "50-agent swarm (replaced by 3 focused models)", "H3 spatial visualization (deleted)", "Frontend dashboard (deleted -- CLI-first)", "WebSocket real-time updates (not needed for CLI tool)"
- "Key Decisions" table: Update outcomes -- mark deleted items as "Killed" with dates. Add new decisions: "Dead code pruning April 8 2026", "CLI-first over dashboard", "Google News RSS as primary over GDELT BigQuery"
- "Context" section: Update to reflect April 8 2026 state. Remove "10 feature branches" reference. Note dead code pruning.

STATE.md changes:
- "Current focus": "Phase 1: Contract Registry + Mapping Policy + Evaluation Ledger"
- "Current Position": Phase 1, not started, Status: Planning
- Add to "Decisions": "Dead code pruning: deleted agents/, simulation/engine.py, circuit_breaker.py, spatial/h3_utils.py, ingestion/gdelt.py, ingestion/dedup.py, db/queries.py, frontend/ directory, 3 dead test files (April 8 2026)"
- Add quick task entry for doc rewrite (this task)
- Update "Last activity" to today
- Update "Session Continuity" section
  </action>
  <verify>
    <automated>grep -c "EventBus\|TickOrchestrator\|50.agent\|H3 spatial\|deck.gl\|WebSocket hook\|agent swarm" .planning/PROJECT.md .planning/STATE.md | grep -v ":0$" | wc -l</automated>
  </verify>
  <done>PROJECT.md and STATE.md contain zero references to deleted/non-existent modules. PROJECT.md validated section only lists modules confirmed in CLAUDE.md. STATE.md reflects current position.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite ROADMAP.md with 5 new phases</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Complete rewrite of ROADMAP.md. Delete ALL existing content. Write new roadmap with these 5 phases:

Overview: Parallax is a working CLI prediction market edge-finder. The pipeline runs end-to-end: news ingestion -> 3 prediction models -> market comparison -> divergence detection -> paper trading. The roadmap strengthens the pipeline's trustworthiness and expands its scope. Build order: contract alignment first (fixes the biggest structural weakness), then prediction persistence (needed for calibration), then paper trading evaluation (proves edge), then deployment hardening, then thesis expansion.

Phase 1: Contract Registry + Mapping Policy + Evaluation Ledger
- Goal: Every trade signal has explicit proposition alignment, proxy quality tracking, and confidence discounting -- replacing the heuristic _map_predictions_to_markets()
- Depends on: Nothing (first phase)
- Requirements: REG-01, REG-02, REG-03, REG-04, REG-05
- Success criteria: (1) Contract registry in DuckDB stores every Kalshi/Polymarket contract with resolution criteria and proxy classification per model type. (2) Mapping policy replaces _map_predictions_to_markets() with explicit proxy-aware decision logic that discounts edge for non-DIRECT mappings. (3) Signal ledger records every signal with full provenance (model claim, contract mapped, proxy class, market state, trade decision). (4) Pipeline runs end-to-end using new contract-aware mapping instead of heuristic ticker matching.
- Reference: .planning/research/contract-mapping/RESEARCH.md

Phase 2: Prediction Persistence + Calibration
- Goal: Every prediction the system makes is persisted with full context, enabling calibration analysis and model improvement
- Depends on: Phase 1 (needs signal ledger schema)
- Requirements: PERS-01, PERS-02, PERS-03, PERS-04
- Success criteria: (1) Every prediction output (probability, reasoning, news context, cascade state) is persisted in DuckDB. (2) Resolution checker polls Kalshi API for settled contracts and backfills outcomes. (3) Calibration queries work: hit rate by proxy class, model calibration curve (are 70% predictions right 70% of the time?), edge decay analysis. (4) At least one week of prediction history is accumulated and queryable.

Phase 3: Paper Trading Evaluation
- Goal: Contract-level P&L tracking proves or disproves the system's edge with statistical significance
- Depends on: Phase 2 (needs prediction persistence + resolution data)
- Requirements: TRAD-01, TRAD-02, TRAD-03
- Success criteria: (1) Paper trades are tracked at contract level with entry price, exit/resolution price, and realized P&L. (2) P&L is segmented by proxy class (DIRECT vs NEAR_PROXY vs LOOSE_PROXY). (3) Summary report shows total P&L, win rate, average edge at entry, and whether edge is statistically significant.

Phase 4: Deployment Fixes
- Goal: The system runs reliably in Docker with hydrated API endpoints and proper error handling
- Depends on: Phase 1 (contract registry needed for API responses)
- Requirements: DEPLOY-01, DEPLOY-02, DEPLOY-03, DEPLOY-04
- Success criteria: (1) docker compose up starts the backend and health check passes. (2) FastAPI endpoints return real data (predictions, markets, divergences, trades) not empty responses. (3) Pipeline handles API failures gracefully (Kalshi rate limits, GDELT 429s, network errors) with retry + fallback. (4) Logging provides clear audit trail of each pipeline run.

Phase 5: Second Thesis Expansion
- Goal: Expand beyond Iran/Hormuz to additional prediction market opportunities (energy/macro)
- Depends on: Phase 3 (must prove edge on first thesis before expanding)
- Requirements: THESIS-01, THESIS-02, THESIS-03
- Success criteria: (1) At least one additional thesis domain identified with active Kalshi/Polymarket contracts. (2) New prediction model added for the second thesis using the same pipeline infrastructure. (3) Contract registry expanded with new contracts and proxy classifications.

Progress table: All 5 phases at 0/TBD, not started.

Execution order: 1 -> 2 -> 3, with 4 parallelizable after 1. Phase 5 after 3.
  </action>
  <verify>
    <automated>grep -c "Phase [1-5]:" .planning/ROADMAP.md</automated>
  </verify>
  <done>ROADMAP.md has exactly 5 phases matching the new architecture. No references to EventBus, TickOrchestrator, agents swarm, H3 viz, frontend panels, WebSocket, or any deleted module.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite REQUIREMENTS.md with new requirement IDs</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Complete rewrite of REQUIREMENTS.md. Delete ALL existing content. Write new requirements matching the 5 roadmap phases.

Header: Defined 2026-04-08. Core Value: Find mispriced prediction market contracts by reasoning about second-order cascade effects -- validated via paper trading P&L.

v1 Requirements (mapped to phases):

Contract Registry (Phase 1):
- REG-01: Contract registry in DuckDB stores ticker, source, event_ticker, title, resolution_criteria, resolution_date, is_active for every tracked contract
- REG-02: Proxy classification per model type (ProxyClass enum: DIRECT, NEAR_PROXY, LOOSE_PROXY, NONE) stored in contract_proxy_map table
- REG-03: MappingPolicy replaces _map_predictions_to_markets() -- evaluates all contracts for each prediction, applies confidence discount by proxy class, refuses NONE mappings
- REG-04: Signal ledger persists every signal (model claim, contract mapped, proxy class, market state, trade decision, resolution outcome) as append-only records
- REG-05: Pipeline integration -- brief.py uses MappingPolicy + SignalLedger instead of heuristic ticker matching

Prediction Persistence (Phase 2):
- PERS-01: Every PredictionOutput (probability, reasoning, news context, cascade inputs) persisted in DuckDB with timestamp and run_id
- PERS-02: Resolution checker polls Kalshi/Polymarket APIs for settled contracts, backfills signal_ledger with resolution_price and realized_pnl
- PERS-03: Calibration queries: hit rate by proxy class, calibration curve by probability bucket, edge decay by effective_edge bucket
- PERS-04: At least 7 days of prediction data accumulated before calibration analysis is considered valid

Paper Trading Evaluation (Phase 3):
- TRAD-01: Paper trades tracked at contract level with entry_price, resolution_price, realized_pnl, hold_duration
- TRAD-02: P&L segmented by proxy_class -- DIRECT, NEAR_PROXY, LOOSE_PROXY reported separately
- TRAD-03: Summary report: total P&L, win rate, avg edge at entry, Sharpe-like ratio, statistical significance test

Deployment Fixes (Phase 4):
- DEPLOY-01: docker compose up starts backend, health check passes within 30 seconds
- DEPLOY-02: FastAPI GET endpoints (/api/predictions, /api/markets, /api/divergences, /api/trades) return real pipeline data
- DEPLOY-03: API failure handling: retry with exponential backoff for Kalshi/GDELT/EIA rate limits, fallback to cached data
- DEPLOY-04: Structured logging (JSON) with run_id correlation across pipeline stages

Second Thesis (Phase 5):
- THESIS-01: Framework for adding new thesis domains: new prediction model + contract registry entries + proxy classifications
- THESIS-02: At least one additional thesis domain (e.g., energy macro, US election, crypto regulation) with active contracts
- THESIS-03: New model integrated into existing pipeline (brief.py, divergence detector, paper trader)

Out of Scope section:
- 50-agent swarm (replaced by 3 focused models -- killed April 2026)
- H3 spatial visualization / deck.gl map (deleted -- CLI tool doesn't need maps)
- Frontend dashboard (deleted -- CLI-first, API endpoints for future UI)
- WebSocket real-time updates (not needed for CLI tool)
- EventBus / TickOrchestrator (never built, pipeline is sequential)
- Multi-user auth (single analyst tool)
- Real-money trading (paper only until edge proven)
- Mobile app
- More than 3 prediction models for Iran thesis (focus on quality over quantity)

Traceability table mapping all requirement IDs to phases.

Coverage: v1 requirements: 19 total, all mapped to phases.
  </action>
  <verify>
    <automated>grep -c "FOUND-\|PIPE-\|FE-\|ANOM-\|CAL-\|PROMPT-\|API-0[1-3]" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>REQUIREMENTS.md has 19 new requirements (REG-*, PERS-*, TRAD-*, DEPLOY-*, THESIS-*) mapped to 5 phases. Zero old requirement IDs (FOUND-*, PIPE-*, FE-*, ANOM-*, CAL-*, PROMPT-*) remain. No references to deleted modules.</done>
</task>

</tasks>

<verification>
After all 3 tasks complete:
1. `grep -rn "EventBus\|TickOrchestrator\|50.agent\|agent swarm\|H3 spatial\|deck.gl\|WebSocket hook\|FOUND-\|PIPE-\|FE-0\|ANOM-\|CAL-0\|PROMPT-0" .planning/PROJECT.md .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md` returns zero matches (no stale references)
2. `grep -c "Phase [1-5]" .planning/ROADMAP.md` returns 5
3. `grep -c "REG-\|PERS-\|TRAD-\|DEPLOY-\|THESIS-" .planning/REQUIREMENTS.md` shows all 19 requirements present
4. ROADMAP phase requirements reference only new IDs (REG-*, PERS-*, TRAD-*, DEPLOY-*, THESIS-*)
</verification>

<success_criteria>
- All 4 planning documents rewritten to reflect the actual CLI prediction market edge-finder architecture
- Zero references to deleted modules (agents/, simulation/engine.py, circuit_breaker.py, spatial/h3_utils.py, frontend/, etc.)
- Zero references to never-built components (EventBus, TickOrchestrator)
- ROADMAP has 5 phases matching the user's specified roadmap
- REQUIREMENTS has new IDs (REG-*, PERS-*, TRAD-*, DEPLOY-*, THESIS-*) with zero old IDs
- PROJECT.md validated section only lists modules that exist in CLAUDE.md's module map
- STATE.md reflects current position accurately
</success_criteria>

<output>
After completion, create `.planning/quick/260408-4ys-rewrite-planning-docs-to-reflect-archite/260408-4ys-SUMMARY.md`
</output>
