---
phase: 10-prompt-fixes-dependency-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
autonomous: true
requirements:
  - ARCH-04

must_haves:
  truths:
    - "pyproject.toml does not list h3, sentence-transformers, searoute, shapely, google-cloud-bigquery, or websockets as dependencies"
    - "pip install -e . succeeds without pulling the 6 removed packages"
  artifacts:
    - path: "backend/pyproject.toml"
      provides: "Clean dependency list without dead packages"
  key_links: []
---

<objective>
Remove 6 dead dependencies from pyproject.toml that are no longer imported anywhere in the backend codebase.

Purpose: These packages add install time (sentence-transformers alone pulls PyTorch), increase attack surface, and confuse contributors into thinking the codebase uses them. None are imported in any production code path.

Output: pyproject.toml with 6 fewer dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-prompt-fixes-dependency-cleanup/10-CONTEXT.md
@.planning/phases/10-prompt-fixes-dependency-cleanup/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove 6 dead dependencies from pyproject.toml</name>
  <files>backend/pyproject.toml</files>
  <read_first>
    - backend/pyproject.toml (current dependency list -- lines 5-21)
    - .planning/phases/10-prompt-fixes-dependency-cleanup/10-RESEARCH.md (ARCH-04 section confirming 6 dead deps and no importers)
  </read_first>
  <action>
**Per ARCH-04 (Claude's discretion):** Remove these 6 lines from the `dependencies` list in pyproject.toml:

1. `"h3>=4.1",` -- was for H3 hex grid, now frontend-only (h3-js)
2. `"sentence-transformers>=3.4",` -- was for embeddings, never used in pipeline
3. `"searoute>=1.3",` -- was for sea route calc, replaced by static config
4. `"shapely>=2.0",` -- was for geometry ops, no current importers
5. `"google-cloud-bigquery>=3.27",` -- was for BigQuery, replaced by DuckDB
6. `"websockets>=14.0",` -- uvicorn has its own WebSocket support

The resulting `dependencies` list should be:
```toml
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.34",
    "duckdb>=1.2",
    "anthropic>=0.52",
    "pydantic>=2.10",
    "pyyaml>=6.0",
    "httpx>=0.28",
    "cryptography>=44.0",
    "truthbrush>=0.2",
]
```

That is 9 remaining dependencies (down from 15).

**Before removing, verify no hidden importers exist:**
Run: `grep -r "import h3\|import sentence_transformers\|import searoute\|import shapely\|from google.cloud import bigquery\|import websockets" backend/src/parallax/`
This should return empty. If any match is found, do NOT remove that dependency.

**After removing, verify install still works:**
Run: `cd backend && pip install -e . --dry-run` (or just verify pyproject.toml parses correctly with `python -c "import tomllib; tomllib.load(open('pyproject.toml', 'rb'))"`)
  </action>
  <verify>
    <automated>cd /Users/adit/Personal\ Projects/parallax-markets/backend && python -c "import tomllib; d = tomllib.load(open('pyproject.toml', 'rb')); deps = d['project']['dependencies']; assert 'h3>=4.1' not in ' '.join(deps); assert 'sentence-transformers' not in ' '.join(deps); assert 'searoute' not in ' '.join(deps); assert 'shapely' not in ' '.join(deps); assert 'google-cloud-bigquery' not in ' '.join(deps); assert 'websockets' not in ' '.join(deps); assert len(deps) == 9; print(f'OK: {len(deps)} dependencies')"</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml does NOT contain "h3>=4.1"
    - pyproject.toml does NOT contain "sentence-transformers"
    - pyproject.toml does NOT contain "searoute"
    - pyproject.toml does NOT contain "shapely"
    - pyproject.toml does NOT contain "google-cloud-bigquery"
    - pyproject.toml does NOT contain "websockets"
    - pyproject.toml dependency list has exactly 9 entries
    - pyproject.toml parses without error (valid TOML)
    - `grep -r "import h3" backend/src/parallax/` returns empty
    - `grep -r "import sentence_transformers" backend/src/parallax/` returns empty
  </acceptance_criteria>
  <done>6 dead dependencies removed from pyproject.toml. 9 production dependencies remain. TOML is valid.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | No trust boundary change -- removing unused packages reduces attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Elevation of Privilege | pyproject.toml | mitigate | Removing unused packages (especially sentence-transformers which pulls PyTorch with native extensions) reduces supply chain attack surface. This IS the mitigation. |
</threat_model>

<verification>
1. `python -c "import tomllib; ..."` -- pyproject.toml parses and has 9 deps
2. `grep -r "import h3\|sentence_transformers\|searoute\|shapely\|bigquery\|websockets" backend/src/parallax/` -- returns empty
3. `cd backend && python -m pytest tests/ -x -q` -- full suite still passes (no test imports removed packages)
</verification>

<success_criteria>
- pyproject.toml lists 9 production dependencies (no dead packages)
- No runtime imports of removed packages exist in backend/src/parallax/
- Test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/10-prompt-fixes-dependency-cleanup/10-03-SUMMARY.md`
</output>
