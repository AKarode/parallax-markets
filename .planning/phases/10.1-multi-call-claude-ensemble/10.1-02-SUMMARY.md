---
phase: "10.1"
plan: "02"
subsystem: prediction
tags: [ensemble, sonnet, worldstate-bugfix, predictor-rewire]
dependency_graph:
  requires: [ensemble.py, world_state.copy(), PredictionOutput ensemble fields]
  provides: [ensemble oil prediction, ensemble ceasefire prediction, ensemble hormuz prediction, worldstate mutation safety]
  affects: [brief.py pipeline, budget tracking, prediction output schema]
tech_stack:
  added: []
  patterns: [multi-call ensemble aggregation, trimmed mean, instability-aware confidence]
key_files:
  created: []
  modified:
    - backend/src/parallax/prediction/oil_price.py
    - backend/src/parallax/prediction/ceasefire.py
    - backend/src/parallax/prediction/hormuz.py
    - backend/tests/test_prediction.py
decisions:
  - "Used world_state.copy() only in oil_price.py; hormuz uses read-only access via get_cell()"
  - "Switched all 3 predictors from Opus to Sonnet (cost reduction from ~$0.63/run to ~$0.063/run)"
metrics:
  duration_seconds: 335
  completed: "2026-04-15T20:46:26Z"
  tasks_completed: 2
  tasks_total: 2
  files_modified: 4
---

# Phase 10.1 Plan 02: Predictor Ensemble Integration Summary

All 3 predictors rewired to use ensemble_predict() with Sonnet 4, WorldState mutation bug fixed in oil predictor.

## What Was Done

### Task 1: Rewire oil_price.py with ensemble + WorldState copy fix (ac3b0e6)
- Replaced single Opus LLM call with `ensemble_predict()` making 3 concurrent Sonnet calls
- **BUG-01 FIX**: Added `world_state.copy()` before cascade operations so `apply_blockade()` mutations do not corrupt the shared WorldState used by the Hormuz predictor
- Added instability check: confidence halved when `ensemble_std_dev > 0.10`
- PredictionOutput now includes `ensemble_probabilities`, `ensemble_std_dev`, `ensemble_is_unstable`
- Reasoning string appended with ensemble metadata (individual probabilities, method, std_dev)
- Added `test_worldstate_not_mutated` test proving shared WorldState is unchanged after prediction
- Updated `test_budget_tracked` assertion from 1 call to 3 calls

### Task 2: Rewire ceasefire.py and hormuz.py with ensemble (f545d85)
- Applied same ensemble_predict pattern to both predictors
- Removed manual JSON parsing (delegated to ensemble.py's `parse_llm_json`)
- Removed `import json` from all 3 predictor files (no longer needed)
- Added instability-aware confidence halving to both
- Added ensemble metadata to PredictionOutput in both
- Hormuz predictor does NOT need WorldState copy -- `_get_hormuz_status` and `_estimate_recovery` use `ws.get_cell()` which returns dict copies

## Verification Results

- **33/33 tests pass** across test_prediction.py, test_ensemble.py, test_world_state.py
- Zero references to `claude-opus-4-20250514` remain in any predictor file
- All 3 predictors import and call `ensemble_predict`
- All 3 predictors use `claude-sonnet-4-20250514`
- All 3 predictors include `confidence *= 0.5` for unstable ensembles
- All 3 predictors populate ensemble metadata in PredictionOutput
- Pre-existing test failures in test_mapping_policy.py and test_recalibration.py confirmed unrelated

## Deviations from Plan

None - plan executed exactly as written.

## Pre-existing Test Failures (Out of Scope)

- `test_mapping_policy.py::TestDirectProxyDiscount::test_direct_proxy_full_edge` -- effective_edge vs raw_edge mismatch (cost subtraction)
- `test_mapping_policy.py::TestNearProxyDiscount::test_near_proxy_discounted_edge` -- confidence_discount expected 0.6, got 1.0
- `test_recalibration.py::TestRecalibrateProbability::test_above_min_signals_adjusts_probability` -- calibrated probability not adjusting

All confirmed pre-existing on base commit dc5c6db.

## Known Stubs

None -- all data paths are fully wired.

## Self-Check: PASSED
