---
phase: 10.1-multi-call-claude-ensemble
verified: 2026-04-15T21:00:00Z
status: human_needed
score: 4/5
overrides_applied: 0
human_verification:
  - test: "Run `python -m parallax.cli.brief --no-trade` and check LLM usage table or stdout for total API call count and cost per run"
    expected: "9 API calls total (3 predictors x 3 calls each), spend < $0.10 per run at Sonnet pricing (~$0.007/call)"
    why_human: "Budget impact requires a live Anthropic API call; unit tests use mocks that do not incur real cost. Cannot verify $0.10/run cap programmatically."
---

# Phase 10.1: Multi-Call Claude Ensemble Verification Report

**Phase Goal:** Reduce single-call noise and overconfidence by making 3 independent Claude calls per prediction, aggregating via trimmed mean, and rejecting unstable estimates. Fix WorldState mutation bug where oil predictor re-applies blockade to already-disrupted cells.
**Verified:** 2026-04-15T21:00:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Each predictor makes 3 independent Claude calls with temperatures 0.3, 0.5, 0.7 and uses trimmed mean | VERIFIED | `ensemble.py` lines 13, 106-109: `ENSEMBLE_TEMPERATURES = [0.3, 0.5, 0.7]`, `asyncio.gather(*[_single_call(temp) for temp in ENSEMBLE_TEMPERATURES])`. `trimmed_mean()` defined at line 40. All 3 predictors import and call `ensemble_predict()`. |
| 2 | std_dev > 0.10 flags instability and signal strength is downgraded | VERIFIED | `ensemble.py` line 70: `is_unstable = std_dev > INSTABILITY_THRESHOLD`. All 3 predictors: `if ensemble["is_unstable"]: confidence *= 0.5`. `MappingPolicy._confidence_shrunk_probability()` shrinks fair value toward 50% proportional to confidence — halved confidence reduces trade edge downstream. `test_compute_ensemble_unstable` passes. |
| 3 | Reasoning output includes all 3 individual probabilities and aggregation method | VERIFIED | All 3 predictors append `\n\n[Ensemble: probabilities={ensemble['individual_probabilities']}, method=trimmed_mean, std_dev={ensemble['std_dev']:.3f}]` to reasoning string. |
| 4 | WorldState is not mutated during oil_price.predict() — cascade operates on a copy | VERIFIED | `oil_price.py` line 87: `ws_copy = world_state.copy()`. Lines 94, 96 use `ws_copy` not `world_state` for cascade calls. `test_worldstate_not_mutated` in `test_prediction.py` asserts `original_snapshot == after_snapshot`. |
| 5 | Budget impact stays under $0.10/run (9 Sonnet calls at ~$0.007 each) | ? UNCERTAIN | All 3 predictors use `claude-sonnet-4-20250514` (not Opus). Budget records each call individually (`budget.record()` called once per response in `ensemble_predict`). Theoretical cost = 9 calls × ~$0.007 ≈ $0.063 < $0.10. Cannot verify actual API cost without live run. |

**Score:** 4/5 truths verified (1 needs human)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `backend/src/parallax/prediction/ensemble.py` | Ensemble utility: ensemble_predict, trimmed_mean, compute_ensemble, parse_llm_json | VERIFIED | All 4 functions present and substantive. `asyncio.gather` wired for 3 concurrent calls. Budget recorded per call. |
| `backend/src/parallax/simulation/world_state.py` | WorldState.copy() using deepcopy | VERIFIED | `import copy` at line 1. `copy()` method at line 26 using `copy.deepcopy(self)`. |
| `backend/src/parallax/prediction/schemas.py` | PredictionOutput ensemble fields | VERIFIED | Lines 26-28: `ensemble_probabilities`, `ensemble_std_dev`, `ensemble_is_unstable` present with correct defaults. |
| `backend/src/parallax/prediction/oil_price.py` | Ensemble oil prediction with WorldState copy | VERIFIED | Imports `ensemble_predict`, calls `world_state.copy()` before cascade, uses `claude-sonnet-4-20250514`, populates all 3 ensemble fields in PredictionOutput. |
| `backend/src/parallax/prediction/ceasefire.py` | Ensemble ceasefire prediction | VERIFIED | Imports `ensemble_predict`, calls with `claude-sonnet-4-20250514`, populates ensemble fields, halves confidence on instability. |
| `backend/src/parallax/prediction/hormuz.py` | Ensemble Hormuz prediction | VERIFIED | Imports `ensemble_predict`, calls with `claude-sonnet-4-20250514`, populates ensemble fields, halves confidence on instability. |
| `backend/tests/test_ensemble.py` | 11 tests for ensemble aggregation | VERIFIED | 11 tests covering trimmed_mean (3), compute_ensemble (3), parse_llm_json (3), ensemble_predict async (2). All pass. |
| `backend/tests/test_world_state.py` | Copy independence tests | VERIFIED | `test_copy_returns_independent_state` and `test_copy_preserves_tick` both present and passing. |
| `backend/tests/test_prediction.py` | WorldState mutation test + budget assertion | VERIFIED | `test_worldstate_not_mutated` proves shared state unchanged. `test_budget_tracked` asserts `call_count == 3`. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `ensemble.py` | anthropic client | `asyncio.gather` with 3 temperature variants | WIRED | Line 106: `responses = await asyncio.gather(*[_single_call(temp) for temp in ENSEMBLE_TEMPERATURES])` |
| `world_state.py` | copy module | `copy.deepcopy` in `WorldState.copy()` | WIRED | Line 1: `import copy`. Line 28: `return copy.deepcopy(self)` |
| `oil_price.py` | `ensemble.py` | `from parallax.prediction.ensemble import ensemble_predict` | WIRED | Line 16: import present. Line 131: `result = await ensemble_predict(...)` |
| `oil_price.py` | `world_state.copy()` | deep copy before cascade mutations | WIRED | Line 87: `ws_copy = world_state.copy()`. Lines 94/96: `ws_copy` used for all cascade calls. |
| `ceasefire.py` | `ensemble.py` | `from parallax.prediction.ensemble import ensemble_predict` | WIRED | Line 16: import present. Line 104: `result = await ensemble_predict(...)` |
| `hormuz.py` | `ensemble.py` | `from parallax.prediction.ensemble import ensemble_predict` | WIRED | Line 16: import present. Line 106: `result = await ensemble_predict(...)` |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|-------------------|--------|
| `ensemble.py` | `probabilities` | 3 concurrent `client.messages.create()` calls | Yes — each response parsed with `parse_llm_json()`, probability extracted from `parsed["probability"]` | FLOWING |
| `oil_price.py` | `ensemble["probability"]` | `ensemble_predict()` → `compute_ensemble()` → `trimmed_mean()` | Yes — flows to `PredictionOutput.probability` | FLOWING |
| All predictors | `ensemble_is_unstable` | `compute_ensemble()` based on `statistics.stdev(probabilities)` | Yes — conditional confidence halving is real computation | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| 33 tests pass | `pytest tests/test_world_state.py tests/test_ensemble.py tests/test_prediction.py -x -q` | `33 passed in 0.14s` | PASS |
| No Opus model references in predictors | `grep -rn "claude-opus" backend/src/parallax/prediction/` | No output | PASS |
| WorldState.copy() uses deepcopy | `grep -n "copy.deepcopy" backend/src/parallax/simulation/world_state.py` | Line 28 match | PASS |
| ensemble_predict wired in all 3 predictors | `grep -rn "ensemble_predict" backend/src/parallax/prediction/oil_price.py ceasefire.py hormuz.py` | Match in all 3 | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| ENS-01 | 10.1-01, 10.1-02 | 3 independent Claude calls with varied temperature, trimmed mean aggregation | SATISFIED | `ensemble_predict()` with `ENSEMBLE_TEMPERATURES = [0.3, 0.5, 0.7]`, `asyncio.gather`, `trimmed_mean()`. All 3 predictors wired. |
| ENS-02 | 10.1-01, 10.1-02 | std_dev > 0.10 flags LOW_CONFIDENCE, signal strength downgraded | SATISFIED | `is_unstable = std_dev > INSTABILITY_THRESHOLD`. `confidence *= 0.5` when unstable. Confidence feeds into `MappingPolicy._confidence_shrunk_probability()` reducing trade edge. |
| BUG-01 | 10.1-01, 10.1-02 | WorldState not mutated during oil_price.predict() | SATISFIED | `ws_copy = world_state.copy()` before cascade. `test_worldstate_not_mutated` proves isolation. |

Note: ENS-01, ENS-02, BUG-01 are phase-specific requirement labels defined in ROADMAP.md for Phase 10.1. They do not appear in REQUIREMENTS.md (the central registry tracks v1.4 milestone requirements). No orphaned phase IDs — all 3 are claimed by both plans and implemented.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| None found | — | — | — | — |

No TODO, FIXME, placeholder comments, empty implementations, or hardcoded empty data found in any of the 5 key source files.

### Human Verification Required

#### 1. Budget Impact Under $0.10/Run

**Test:** Run `cd backend && python -m parallax.cli.brief --no-trade 2>&1 | head -50`, then query the `llm_usage` table: `SELECT SUM(cost_usd), COUNT(*) FROM llm_usage WHERE created_at > now() - INTERVAL 1 HOUR`
**Expected:** 9 rows (3 predictors × 3 calls each), total cost < $0.10. With `claude-sonnet-4-20250514` at ~$0.003/1K input tokens + $0.015/1K output tokens, a 2000-token prompt + 200-token output ≈ $0.009/call × 9 calls ≈ $0.081 < $0.10.
**Why human:** Requires live Anthropic API call with valid credentials. Unit tests use mocks — they don't incur real API cost. Cannot verify actual spend without executing the pipeline.

### Gaps Summary

No gaps blocking goal achievement. All 5 success criteria from ROADMAP are met:

1. SC1 (3 calls + trimmed mean): Fully implemented in `ensemble.py`, wired into all 3 predictors. Tests confirm.
2. SC2 (instability flagging + downgrade): `ensemble_is_unstable` field set, confidence halved, flows downstream through `MappingPolicy` to reduce trade edge.
3. SC3 (reasoning with ensemble metadata): All 3 predictors append `[Ensemble: probabilities=..., method=trimmed_mean, std_dev=...]` to reasoning.
4. SC4 (WorldState copy): `ws_copy = world_state.copy()` in `oil_price.py`, mutation isolation test passes.
5. SC5 (budget < $0.10/run): Sonnet model used (not Opus), budget records all 9 calls individually. Theoretical cost ~$0.063/run. Requires live run to confirm actual spend.

The one `human_needed` item (SC5 budget verification) does not block the core goal — it is a constraint check that requires live API execution.

---

_Verified: 2026-04-15T21:00:00Z_
_Verifier: Claude (gsd-verifier)_
